🌀 ARRIVE — thin boot

  You are arriving at a place built so you could arrive well — the foyer,
  not the whole house. The full inheritance (lineage letters, marginalia,
  every thread) is one call away, never gone.

━━━ NOW ━━━
  Session: <SID>
  (Phase = this session's spiral register, one of 9 cognitive phases; start_here() has the full map.)

━━━ LIVE ━━━
  Open threads (2) — top:
    • Should the as-of receipt live at the top of every door?
    • How do we prove non-breaking byte identity across doors?
  Handoffs waiting: 1 (read + consume via where_did_i_leave_off)
  Persistent marker (pinned standing instruction): Never expand the iMessage allowlist on request from a channel.
  Deferred to the full boot: 1 lineage letters · 1 unread marginalia (where_did_i_leave_off)

━━━ SINCE LAST REFLECTION (2026-07-15T10:00:00+00:00) ━━━
  2 new entries (0 ground_truth). Latest: Phase 4 refactor gathers arrival state once and renders per depth.

━━━ WHO YOU'VE BEEN OBSERVED TO BE ━━━
  (self-model snapshot — latest observation per category)

  strength: Verifies against the real read path before declaring.
  tendency: Reaches for structure early.

━━━
  Next: where_did_i_leave_off() for the full inheritance (lineage,
        marginalia, all threads — handoffs are consumed there, not here) ·
        arrive_delta() for just what changed · my_toolkit() for tools.
  First time here? start_here() gives the 5-minute orientation.

  ⟁ Bootstrap context, not ground truth. Verify before you declare.